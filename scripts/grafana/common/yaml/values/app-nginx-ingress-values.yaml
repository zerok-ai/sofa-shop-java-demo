controller:
  replicaCount: 1
  nodeSelector: {
    kubernetes.io/os: linux
  }
  metrics:
    enabled: true
    serviceMonitor: 
      enabled: true
      additionalLabels: {
        release: "prometheus"
      }
defaultBackend:
  nodeSelector: {
    kubernetes.io/os: linux
  }
  